import java.util.*;

public class Q3 {
    public static void swap(String num, int bit1, int bit2) {
        if (bit1 > num.length() || bit2 > num.length())
            return;
        if (num.charAt(0) == '-')
            num = num.substring(1, num.length());
        int count = 0;
    }
}
